# Base URL of the CV service (Phase 1 mock SSE service).
# The frontend connects to `${VITE_CV_SERVICE_URL}/events`.
# Default when unset: http://127.0.0.1:8000
VITE_CV_SERVICE_URL=http://127.0.0.1:8000
